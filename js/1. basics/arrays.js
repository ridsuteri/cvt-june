let array = [70, 65, 43, 93];

// accessing the elements of array
// console.log(array[0]);
// console.log(array[3]);

// adding & removing elements to array

// array.push(76)
// array.pop();
// array.pop();

// adding & removing from right/left

// removes from left(front)
// array.shift()

// adds from left(front)
// array.unshift(56)


// splice(startIndex, how much to delete, items to add ...  )

array.splice(0, 2);
// array.splice(2, 0, 0, 1, 2)


console.log(array);